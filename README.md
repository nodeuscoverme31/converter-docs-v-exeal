# converter-docs-v-exeal

Portable Windows utility project for converting Microsoft Word documents (`.doc` / `.docx`) into working Excel workbooks (`.xlsx`) without requiring Microsoft Office or internet access at runtime.

> Bootstrap is in progress. Canonical project documentation and verified commands are being materialized in this repository during Phase 07.
